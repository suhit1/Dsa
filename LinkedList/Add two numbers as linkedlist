// Problem link :- https://leetcode.com/problems/add-two-numbers/

// brute

ex:- 2->4->3
     5->6->4

Simply traverse both the linkedlist and store the elements inside a string

like str1 = "243"
     str2 = "564"
after this parse both of them into int and add them
will get 807 and now again convert this into String "807" and then reverse this make it 708
now after this make a new linkedlist and put 708 in it this is our answer

// optimised

class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        ListNode dummy = new ListNode();
        ListNode temp = dummy;
        int carry = 0;
        while(l1!=null || l2!=null || carry==1){
            int sum=0;
            // check if l1 is not null add it sum
            if(l1!=null){
                sum += l1.val;
                l1 = l1.next;
            }
            // check if l1 is not null add it sum
            if(l2 != null){
                sum += l2.val;
                l2 = l2.next;
            }
            // adding carry to sum
            sum+=carry;
            carry = sum/10;
            ListNode node = new ListNode(sum%10);
            temp.next = node;
            temp = temp.next;
        }
        return dummy.next;
    }
}